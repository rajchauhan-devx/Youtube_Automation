// Shared without importing the Python worker into other generation services.
export const presenterState = { busy: false, mediaRequests: 0, imageRequests: 0, speechRequests: 0, editingRequests: 0, localRequests: 0 };
