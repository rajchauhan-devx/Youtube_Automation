Map the ordered source-image scenes to narration using their actual image descriptions, prompts and the supplied token IDs. Return one start token for every supplied scene, preserving scene order and covering the whole narration. The first scene begins at the first token. Each later scene must start on a distinct later token. Prefer meaningful phrase boundaries; repeated phrases are distinguished by token ID. Never invent a timestamp or scene/image identity.
